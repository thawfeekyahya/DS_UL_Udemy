#ifndef INTRO_H
#define INTRO_H

template <class T>
class Intro {
    private:
        T a,b;
    public:
        Intro(T a,T b);

        // Intro(T a,T b) {
        //     this->a = a;
        //     this->b = b;
        // }
        //T add(T a, T b);
        //T subtract(T a,T b);
};

#endif //INTRO_H